services:
  gateway:
    image: docker/mcp-gateway
    command: ["--servers=duckduckgo", "--transport=sse", "--port=8811"]
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "8811:8811"
